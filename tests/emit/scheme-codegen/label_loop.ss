;; Ziku Runtime Support
;; Generated by Ziku Scheme Backend

;; Pretty print result (matching IR eval output format)
(define (ziku-print-result v)
  (cond
    ((boolean? v) (display (if v "true" "false")))
    ((null? v) (display "()"))
    ((string? v)
     ;; Strings: output with double quotes like IR eval
     (display "\"")
     (display v)
     (display "\""))
    ((char? v)
     ;; Characters: output with single quotes like IR eval
     (display "'")
     (display v)
     (display "'"))
    ((pair? v)
     (if (and (pair? (car v)) (symbol? (caar v)))
         ;; Record
         (begin
           (display "{ ")
           (let loop ((fields v))
             (unless (null? fields)
               (display (caar fields))
               (display " = ")
               (ziku-print-result (cdar fields))
               (unless (null? (cdr fields))
                 (display ", "))
               (loop (cdr fields))))
           (display " }"))
         ;; Regular pair/list
         (begin
           (display "(")
           (ziku-print-result (car v))
           (display " . ")
           (ziku-print-result (cdr v))
           (display ")"))))
    ((procedure? v) (display "<function>"))
    (else (display v)))
  (newline))

;; Logical operators as functions (Scheme's and/or are special forms, not procedures)
(define (ziku-and a b) (and a b))
(define (ziku-or a b) (or a b))

;; Not-equal operator (works for all types)
(define (ziku-ne a b) (not (equal? a b)))

;; Main program
(define (ziku-main halt)
  (let ((_alpha0 halt)) (let ((loop (letrec ((loop (lambda (n) (lambda (_alpha1) (let ((%p (lambda (acc) (lambda (_alpha2) (let ((done _alpha2)) (let ((_alpha3 done)) (let ((_alpha4 (lambda (_f0) (if _f0 (let ((_alpha5 _alpha3)) (let ((%p acc)) (if (and (vector? %p) (eq? (vector-ref %p 0) 'ziku-thunk)) ((vector-ref %p 1) done) (done %p)))) (let ((_alpha6 _alpha3)) (let ((_alpha7 (lambda (_f3) (let ((_alpha9 (lambda (_f4) (let ((%p _f3)) (if (and (vector? %p) (eq? (vector-ref %p 0) 'ziku-thunk)) ((vector-ref %p 1) (lambda (%fn0) (let ((%arg _f4)) (if (and (vector? %arg) (eq? (vector-ref %arg 0) 'ziku-thunk)) ((vector-ref %arg 1) (lambda (%v) ((%fn0 %v) _alpha6))) ((%fn0 %arg) _alpha6))))) ((lambda (%fn0) (let ((%arg _f4)) (if (and (vector? %arg) (eq? (vector-ref %arg 0) 'ziku-thunk)) ((vector-ref %arg 1) (lambda (%v) ((%fn0 %v) _alpha6))) ((%fn0 %arg) _alpha6)))) %p)))))) (_alpha9 (+ acc n)))))) (let ((_f1 loop)) (let ((_alpha8 (lambda (_f2) (let ((%p _f1)) (if (and (vector? %p) (eq? (vector-ref %p 0) 'ziku-thunk)) ((vector-ref %p 1) (lambda (%fn1) (let ((%arg _f2)) (if (and (vector? %arg) (eq? (vector-ref %arg 0) 'ziku-thunk)) ((vector-ref %arg 1) (lambda (%v) ((%fn1 %v) _alpha7))) ((%fn1 %arg) _alpha7))))) ((lambda (%fn1) (let ((%arg _f2)) (if (and (vector? %arg) (eq? (vector-ref %arg 0) 'ziku-thunk)) ((vector-ref %arg 1) (lambda (%v) ((%fn1 %v) _alpha7))) ((%fn1 %arg) _alpha7)))) %p)))))) (_alpha8 (- n 1)))))))))) (_alpha4 (equal? n 0))))))))) (if (and (vector? %p) (eq? (vector-ref %p 0) 'ziku-thunk)) ((vector-ref %p 1) _alpha1) (_alpha1 %p))))))) loop))) (let ((_alpha10 _alpha0)) (let ((_alpha11 (lambda (%fn2) (let ((%arg 0)) (if (and (vector? %arg) (eq? (vector-ref %arg 0) 'ziku-thunk)) ((vector-ref %arg 1) (lambda (%v) ((%fn2 %v) _alpha10))) ((%fn2 %arg) _alpha10)))))) (let ((%p loop)) (if (and (vector? %p) (eq? (vector-ref %p 0) 'ziku-thunk)) ((vector-ref %p 1) (lambda (%fn3) (let ((%arg 5)) (if (and (vector? %arg) (eq? (vector-ref %arg 0) 'ziku-thunk)) ((vector-ref %arg 1) (lambda (%v) ((%fn3 %v) _alpha11))) ((%fn3 %arg) _alpha11))))) ((lambda (%fn3) (let ((%arg 5)) (if (and (vector? %arg) (eq? (vector-ref %arg 0) 'ziku-thunk)) ((vector-ref %arg 1) (lambda (%v) ((%fn3 %v) _alpha11))) ((%fn3 %arg) _alpha11)))) %p))))))))

;; Run with result printer
(ziku-main ziku-print-result)
