-- Lisp S-expressions: basic constructors
-- Test creating Nil, Num, Sym, Cons

let nil = Nil in
let n = Num(42) in
let s = Sym("hello") in
let c = Cons(Num(1), Cons(Num(2), Nil)) in

-- Test extracting a value with pattern matching
match n {
| Num(x) => x
| Nil => 0
| _ => -1
}
