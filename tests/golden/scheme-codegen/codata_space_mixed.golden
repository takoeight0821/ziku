;; Ziku Runtime Support
;; Generated by Ziku Scheme Backend

;; Pretty print result (matching IR eval output format)
(define (ziku-print-result v)
  (cond
    ((boolean? v) (display (if v "true" "false")))
    ((null? v) (display "()"))
    ((string? v)
     ;; Strings: output with double quotes like IR eval
     (display "\"")
     (display v)
     (display "\""))
    ((char? v)
     ;; Characters: output with single quotes like IR eval
     (display "'")
     (display v)
     (display "'"))
    ((pair? v)
     (if (and (pair? (car v)) (symbol? (caar v)))
         ;; Record
         (begin
           (display "{ ")
           (let loop ((fields v))
             (unless (null? fields)
               (display (caar fields))
               (display " = ")
               (ziku-print-result (cdar fields))
               (unless (null? (cdr fields))
                 (display ", "))
               (loop (cdr fields))))
           (display " }"))
         ;; Regular pair/list
         (begin
           (display "(")
           (ziku-print-result (car v))
           (display " . ")
           (ziku-print-result (cdr v))
           (display ")"))))
    ((procedure? v) (display "<function>"))
    (else (display v)))
  (newline))

;; Main program
(define (ziku-main halt)
  (let ((_alpha0 halt)) (let ((obj (lambda (x) (lambda (_alpha1) (let ((%p (lambda (y) (lambda (_alpha2) (let ((%p (list (cons 'value (vector 'ziku-thunk (lambda (_alpha3) (_alpha3 (+ x y)))))))) (if (and (vector? %p) (eq? (vector-ref %p 0) 'ziku-thunk)) ((vector-ref %p 1) _alpha2) (_alpha2 %p))))))) (if (and (vector? %p) (eq? (vector-ref %p 0) 'ziku-thunk)) ((vector-ref %p 1) _alpha1) (_alpha1 %p))))))) (let ((_alpha4 _alpha0)) (let ((_alpha5 (lambda (%rec) (let* ((%is-dispatch (and (vector? %rec) (eq? (vector-ref %rec 0) 'ziku-dispatch))) (%v (cond (%is-dispatch ((vector-ref %rec 1) 'value)) ((procedure? %rec) (%rec 'value)) (else (cdr (assq 'value %rec)))))) (if (and (vector? %v) (eq? (vector-ref %v 0) 'ziku-thunk)) ((vector-ref %v 1) _alpha4) (_alpha4 %v)))))) (let ((_alpha6 (lambda (%fn0) (let ((%arg 20)) (if (and (vector? %arg) (eq? (vector-ref %arg 0) 'ziku-thunk)) ((vector-ref %arg 1) (lambda (%v) ((%fn0 %v) _alpha5))) ((%fn0 %arg) _alpha5)))))) (let ((%p obj)) (if (and (vector? %p) (eq? (vector-ref %p 0) 'ziku-thunk)) ((vector-ref %p 1) (lambda (%fn1) (let ((%arg 10)) (if (and (vector? %arg) (eq? (vector-ref %arg 0) 'ziku-thunk)) ((vector-ref %arg 1) (lambda (%v) ((%fn1 %v) _alpha6))) ((%fn1 %arg) _alpha6))))) ((lambda (%fn1) (let ((%arg 10)) (if (and (vector? %arg) (eq? (vector-ref %arg 0) 'ziku-thunk)) ((vector-ref %arg 1) (lambda (%v) ((%fn1 %v) _alpha6))) ((%fn1 %arg) _alpha6)))) %p)))))))))

;; Run with result printer
(ziku-main ziku-print-result)
