description = "Test and debug Ziku expressions through different compilation phases"
prompt = """
Read the skill file at `.agent/skills/test/SKILL.md` and follow its instructions to test Ziku code.

The skill file contains:
- Usage of `./scripts/ziku-test.sh` script
- Available test phases: parse, infer, eval, translate, scheme, scheme-run
- Examples for different testing scenarios
- Common debugging patterns

Use this to quickly test expressions during development or debugging.
"""
